# Asset 開発規約

## コミット先

- `master` へ直接コミットしない。コミット前に現在のブランチを確認し、`master` 上なら作業用ブランチを作成・切替してからコミットする。既存の作業用ブランチと未コミット変更は保持する。

## 作業に必要な知識を選ぶ

実装・レビュー・不具合調査では、ユーザーから読むよう指示されなくても、依頼内容と対象コードから関連領域を判断し、`docs/knowledge/README.md` と下記の対応表から必要な文書を選んで読む。文書を選ぶための許可や、ユーザーによるファイル名の指定は不要である。

- 対象領域の設計・修正・レビューを判断する前に、該当文書の本文を読み、そこで示された契約を現行コードと照合する。リンクや見出しを見ただけで読み込み済みとしない。
- 調査中に依存領域や対象 repo が増えたら、追加領域の入口と必要な未読文書も読む。同一セッションで読んだ文書は、更新やブランチ変更がなければ繰り返し全文を読む必要はない。
- 読み取り専用の依頼にも参照規約を適用するが、編集禁止は維持する。

## 適用範囲

このファイルは `Asset/` datapack と `scripts/` の実装・レビュー・調査に適用する。データパックのコードは repo 内の `Asset/data/` にある。ID ディレクトリ、alias、生成物を変更する場合は、対象領域の知識文書を先に読む。

## 必須の実装規約

- Mob／Object は型定義・継承・メソッド・個体 Field を持つ実行モデルである。変更対象だけでなく親の定義、alias、本体の呼び出し境界を読む。Effect は別方式の継承・固定イベントで動き、Artifact とも同一視しない。
- artifact / effect / mob / object は 4 桁 ID と名前のディレクトリを使う。既存の番号・命名・相対パスを再利用し、重複 ID を作らない。
- datapack の関数は `#>` と `# @within` の宣言を既存例に合わせる。タグから呼ばれる関数は `tags/functions/{artifact,mob,object}` と実装の両方を確認する。
- 登録値は `data modify storage asset:artifact|asset:mob|asset:object ...` に書く。API の引数・戻り値・実行 context は API と既存呼出例ごとに確認し、Return を提供する API だけ `api: Return` を読む。
- Mob／Object／Effect の `asset:context this` は、永続する個体・効果の Field を処理中に展開する領域で、本体が読み出し・書き戻しを担う。一時変数扱いで全削除せず、イベント入力（Attack/Hurt 等）とも区別する。
- artifact の自動使用は ID 検査、共通条件、共通 use の流れを既存の `common/check_condition/auto` と `common/use/auto` に合わせる。手動使用・装備検知など別トリガーは既存同種を参照する。
- Mob／Object のメソッド追加は親からの自動 fallback を止め得る。自前実装・継承・明示 `super.*` を区別し、親が要求する Field と初期化順を守る。`enroll_pool` の参加登録は型定義と分けて確認する。AJ 連携も継承したライフサイクルを含めて読む。
- 広域 `@e` 検索では distance/dx 等で探索範囲を明示し、単一対象を選ぶ `@e` は既存例に合わせて limit/sort を付ける。scoreboard の Temporary は後続の計算範囲を明示する。
- 具体型の `register` は実行時に評価される型定義であり、名前だけで生成物と扱わない。対象の生成・移行スクリプトと履歴から編集方法を確認し、スクリプト実行後は型定義や alias への影響をレビューする。
- ProjectTSB Wiki は命名・作成方針などの設計意図の根拠として使う。ただし旧テンプレートのパス、API 契約、abstract/継承の実行保証は現行コードを優先し、`docs/knowledge/wiki-crosscheck.md` の採否を確認する。

## 対象領域と必読文書

以下は変更だけでなく、その領域のレビュー・調査にも適用する。

- 実装・レビュー・調査共通: `docs/knowledge/README.md`, `docs/knowledge/sources.md`
- Mob／Object／Effect の定義・継承・Field・メソッド変更: 最初に `docs/knowledge/object-model.md`、次に該当領域の文書。
- artifact: `docs/knowledge/artifact.md`
- mob / AJ: `docs/knowledge/mob.md`
- object / API / 生成・検証: `docs/knowledge/runtime-and-tools.md`
- Wiki由来の作成規約・旧仕様との差: `docs/knowledge/wiki-crosscheck.md`
- スクリプト変更: `scripts/` の対象スクリプト本文と `runtime-and-tools.md`

## 検証

PR 前に対象ファイルの参照先、storage の Argument/Return、ID とタグを検索する。生成スクリプトを使った場合は作用先の差分を確認し、冪等と確認できるスクリプトだけ再実行して差分が安定することを確かめる。`update-artifact-register.scala.sc` のような追記型は無条件に再実行しない。Minecraft 実機依存の挙動は CI の成功だけで確定しない。

## 作業中の学びを残す

- 実装、レビューのみの作業、不具合調査、ユーザーからの訂正で、次の実装・レビューに使える知見を得たら、別途「記録して」と頼まれるのを待たず、その作業内で対象の `docs/knowledge/` と `sources.md` を更新する。
- 記録には適用範囲、誤実装・見落としにつながる前提、正しい扱い、根拠のコード／PR等、確認方法と実施範囲を残す。ユーザーの開発方針、現行コードの事実、未確定の提案を区別する。推測を確定規約へ昇格させない。
- 既存の誤記は元の説明と関連文書を訂正する。同じ注意を追記し続けず、次に読む入口から正しい説明へ到達できるようにする。恒常規約なら AGENTS.md、新しい領域なら README の案内も更新する。
- 完了報告の前に、再利用できる発見・訂正の記録漏れ、既存文書との矛盾、根拠リンクを確認する。報告には更新先を短く示す。更新不要なら新規知見がなかった等の理由を一言添える。記録のためだけに知見や未確認事項を作らない。
- 明示的に読み取り専用・ファイル変更禁止を指定された作業では編集せず、残すべき知見と記録先を報告する。記録はこの repo の変更として扱い、commit／push の権限を拡張しない。対象やブランチを変更した後は必読文書を再確認する。
