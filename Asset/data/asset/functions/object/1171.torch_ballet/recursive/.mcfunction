#> asset:object/1171.torch_ballet/recursive/
#
# 継承先などから実行される処理
#
# @within asset:object/alias/1171/recursive

# 何らかの理由で埋まってたら消える
    execute unless block ~ ~ ~ #lib:no_collision/ unless block ^ ^ ^-0.5 #lib:no_collision/ run return run kill @s

# 演出
    particle flame
