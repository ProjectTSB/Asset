#> asset:object/2067.watermelon/recursive/
#
# 継承先などから実行される処理
#
# @within asset:object/alias/2067/recursive

# 演出
    particle dust 0.173 1 0.243 1.3 ~ ~ ~ 0.4 0.4 0.4 0 1 normal @a

# 少しずつ下を向く
    execute at @s run tp @s ^ ^ ^ ~ ~0.5
