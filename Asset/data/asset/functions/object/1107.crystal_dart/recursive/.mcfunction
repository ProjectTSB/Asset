#> asset:object/1107.crystal_dart/recursive/
#
# 継承先などから実行される処理
#
# @within asset:object/alias/1107/recursive

# 何らかの理由で埋まってたら消える
    execute unless block ~ ~ ~ #lib:no_collision/ unless block ^ ^ ^-0.5 #lib:no_collision/ run return run kill @s

# 演出
    particle dust 1 0.671 0.957 1
